问题点：
node执行机制各个阶段
原型，原型链，如何实现继承
跨域jsonp方式实现
vite和webpack相关知识比较
前端攻击防御
  XSS
 --恶意脚本读取网站重要信息
 --会话劫持，比如一些密码，验证码
 --钓鱼重定向
 --调用浏览器功能比如地理位置。摄像头访问
 --影响网站seo，篡改页面、影响用户留存
核心：1. 攻击者提交恶意代码。 2. 浏览器执行恶意代码。
 防范：输入过滤、输出转义、csp内容安全策略（开发者明确告诉客户端，哪些外部资源可以加载和执行，等同于提供白名单）

 csrf
 跨站请求伪造，利用受害者访问目标网站，并发起一些操作，利用受害者在原来网站已登录的状态，来进行攻击，
  预防：
  --同源检测，samesite cookie  （Origin Header  Referer Header）
  --进行双重验证 增加token验证，才能获取或者提交一些重要内容

浏览器相关的知识

http各个版本相关的知识
--超文本安全传输协议， 采用数字证书和数字签名的方式（非对称加密的方式）进行建立连接认证，采用对称加密进行数据传输。

鉴权相关的知识
--Cookie + Session
--JWT（JSON Web Token）
--OAuth
--SSO：关联多个应用登录状态的一种方案
--基于短信或邮件的 OTP（一次性密码）
--指纹、面部识别等生物识别
微前端

Vue实现原理
ssr

